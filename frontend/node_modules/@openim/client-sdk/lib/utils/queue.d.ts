import { MessageItem } from '../types/entity';
export declare class MessageQueue {
    private queue;
    private pending;
    enqueue(task: () => Promise<MessageItem>): Promise<MessageItem>;
    cancelTasks(): void;
    private dequeue;
}
