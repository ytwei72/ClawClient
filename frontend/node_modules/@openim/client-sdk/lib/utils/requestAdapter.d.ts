import { AppPlatform } from '../types/enum';
type AdaptMethod = 'POST' | 'GET';
interface AdaptReq {
    url: string;
    data: unknown;
    platform: AppPlatform;
    headers: Record<string, string>;
    method?: AdaptMethod;
}
type RetryOptions = {
    retries?: number;
    baseDelayMs?: number;
    maxDelayMs?: number;
};
export declare const adaptRequest: (req: AdaptReq, retryOptions?: RetryOptions) => Promise<any>;
export {};
