import { MixinApiService } from '../..';
declare class RelationSyncer {
    private instance;
    private syncedFriendsVersion;
    private syncedFriendsVersionID;
    constructor(instance: MixinApiService);
    reset: () => void;
    syncFriendVersion: (operationID: string) => Promise<void>;
    private compareVersionAndTrigger;
}
export default RelationSyncer;
